# Copie para .env e preencha com os mesmos valores do App Registration
# usado no backend (Client ID e Tenant ID não são segredo — ficam
# expostos no bundle do frontend de qualquer forma).
VITE_AZURE_CLIENT_ID=
VITE_AZURE_TENANT_ID=
